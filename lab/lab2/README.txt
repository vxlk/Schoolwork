Uses hashing to build a 2d grid of strings

Once a word is typed in, it will hash to a location,
	The program will then step down the linked list and print to the user all of the neighbors contained at the 	hashed address (a list of common words)