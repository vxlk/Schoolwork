A simple binary search tree implementation

Supports: split
	  join
	  find
	  select (find by ranking)
	  print
	  and two different versions of insert, one that keeps the tree balanced by random assignment (with high 		  probability of keeping operations nlogn) and one that simply inserts according to key