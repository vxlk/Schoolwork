A modification of the binary search tree code to do all operations according to rank in the tree.
	Therefore every operation is a modification of the select function.

Supports: insert and insert keep balanced
	  select
	  find by rank
	  split
	  A function that tests rank re-ordering, updated when a "team" wins or loses
	  